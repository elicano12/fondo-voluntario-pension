const enviarSMS = require("./enviarSMS");
const enviarEmails = require("./enviarEmails");
const identificadorUnico = require("./identificadorUnico");

module.exports = {
  enviarSMS,
  enviarEmails,
  identificadorUnico,
};
